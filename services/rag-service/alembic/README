Block 4 currently owns no persistence tables. The retrieval port is technology-independent.

pgvector is not enabled in EKA. A future migration must be separate from Block 3 and add a VECTOR(384) column only after the shared PostgreSQL extension and schema contract are approved.
