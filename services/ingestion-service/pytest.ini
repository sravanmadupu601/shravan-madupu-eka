[pytest]
pythonpath = .
testpaths = tests
filterwarnings =
    ignore:The `anyio\\.abc\\.BlockingPortal` alias is deprecated.*:DeprecationWarning
markers =
    integration: tests that load the local Sentence Transformer model
