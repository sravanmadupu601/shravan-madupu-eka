[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "eka-ai"
version = "0.1.0"
description = "EKA-owned AI capability interfaces"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
include = ["eka_ai*"]
